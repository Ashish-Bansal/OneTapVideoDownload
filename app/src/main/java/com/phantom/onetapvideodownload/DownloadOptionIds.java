package com.phantom.onetapvideodownload;


public enum DownloadOptionIds {
    Filename(0), DownloadLocation(1), Format(2);

    private final int mValue;

    DownloadOptionIds(int value) {
        mValue = value;
    }

}
